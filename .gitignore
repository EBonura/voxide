/dist/
/captures/
/game/target/
.DS_Store

# Imported by tools/bootstrap-components.py from components.lock.json. Never
# committed: the lock is the source of truth, this is a cache.
.psoxide/

# Work dir of the retired per-game `make pgo` (psoxide-pgo's driver now works
# under game/target); still ignored so an old checkout's leftovers stay out.
/.pgo/
